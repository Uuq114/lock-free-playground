# lock-free playground

This repository is used to store personal implementations of some lock-free data structures, currently including the following:

- queue

## Quickstart

```bash
mkdir build
cd build
cmake ..
make
```